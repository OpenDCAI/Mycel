/**
 * JS mirror of CSS motion tokens (motion-tokens.css).
 * Use these constants in setTimeout / JS animation APIs where CSS vars aren't available.
 *
 * IMPORTANT: Keep in sync with motion-tokens.css — source of truth is CSS.
 */

// Duration (ms)
export const DURATION_INSTANT = 100;
export const DURATION_FAST = 150;
export const DURATION_NORMAL = 250;
export const DURATION_SLOW = 400;
export const DURATION_DELIBERATE = 600;
